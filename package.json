{
  "name": "website-redesign-outreach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Productized website redesign outreach: concept previews, review dashboard, and personalized outreach drafts.",
  "scripts": {
    "build": "node scripts/build-dashboard.mjs",
    "check": "node scripts/check-concepts.mjs",
    "smoke": "node scripts/smoke.mjs",
    "verify": "npm run build && npm run check && npm run smoke",
    "deploy": "wrangler pages deploy site --project-name=redesign-concepts",
    "fb:post": "node scripts/fb-post.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
